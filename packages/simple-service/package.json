{
  "name": "simple-service",
  "version": "1.3.0",
  "license": "UNLICENSED",
  "main": "dist/src/index.js",
  "private": true,
  "files": [
    "src",
    "__tests__",
    "dist"
  ],
  "scripts": {
    "build": "yarn --cwd ../.. build",
    "build:docker": "docker build -f Dockerfile -t stavalfi/simple-service ../..",
    "test": "jest",
    "prepublishOnly": "yarn --cwd ../.. test && yarn build && yarn version --minor"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "chance": "^1.1.5",
    "express": "^4.17.1",
    "k8test-log": "^1.0.0",
    "lodash": "^4.17.15"
  }
}
