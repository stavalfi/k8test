{
  "name": "k8s-api",
  "version": "1.2.0",
  "license": "UNLICENSED",
  "main": "dist/src/index.js",
  "files": [
    "src",
    "__tests__",
    "dist"
  ],
  "scripts": {
    "build": "tsc -b tsconfig-build.json",
    "test": "jest",
    "prepublish": "yarn --cwd ../.. test && yarn --cwd ../.. build && yarn version --minor"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  },
  "dependencies": {
    "chance": "^1.1.5",
    "lodash": "^4.17.15"
  }
}
